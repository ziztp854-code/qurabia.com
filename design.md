# Design — تحدّي Prestige Live

نظام بصري موحّد لكل أسطح تحدّي، مستمد من معاينة `v18` المعتمدة. المنتج يبدو كمنصة بث مسابقات عربية راقية: أسود عميق، ذهب ملموس، سماوي حي، وهندسة دقيقة أكثر من الزجاجية العامة.

## Thesis

الجولة تبدأ من رمز واحد وتتحول إلى مسرح مباشر. ترفض الواجهة ترتيب صفحات SaaS المعتاد والبطاقات البيضاء المتشابهة؛ كل سطح يُبنى كجزء من غرفة بث واحدة، مع بقاء المهمة واضحة.

## Visual world

- المشهد: استوديو مسابقات عربي ليلي بإضاءة ذهبية وسماوية، يُستخدم من هاتف اللاعب وشاشة المضيف.
- الخلفية `#06020a` مع طبقات الأسطح `#10081a` و`#180e24` و`#0a0412`.
- الذهب `#ffb74d` للإجراء والدعوة الأساسية (وسلّم `--gold-100…900` حول `#d4af37` للهوية)، والسماوي `#00d4ff` محصور في مسارح البث المباشر.
- الأسطح صلبة وداكنة مع حدود رفيعة؛ الزجاجية محصورة في الشريط العلوي والطبقات الطافية.
- التكوين يعتمد الخطوط الأفقية، الشقوق الضوئية، لوحات القياس، وأرقام كبيرة ذات حضور مادي.

## Macrostructure

- **Prestige Split:** الرئيسية وصفحات التسويق؛ عرض كبير بجانب غرفة مباشرة حقيقية أو حالة فارغة صادقة.
- **Control Deck:** صفحات المستخدم والمضيف والإدارة؛ شريط علوي ثابت، مساحة عمل واضحة، وقائمة جانبية عملية.
- **Live Stage:** اللعب والبث والنتائج؛ محتوى أحادي التركيز، وقت وحالة ظاهران، وتباين أعلى.
- **Ceremony Stage:** التتويج؛ منصة ثلاثية الأبعاد محدودة التكلفة، ضوء مركزي، ومراتب ذهبية/فضية/برونزية.

## Tokens

المصدر الوحيد هو `apps/web/tokens.css`؛ لا قيم لون مباشرة داخل المكوّنات. القيم الفعلية للوضعين:

- `--background: #06020a` داكن / `#fefbf6` فاتح
- `--surface: #10081a` داكن / `#f8f1e5` فاتح
- `--surface-strong: #180e24` داكن / `#ffffff` فاتح
- `--foreground: #fff8ed` داكن / `#111827` فاتح
- `--muted-foreground: #a08070` داكن / `#6b6256` فاتح
- `--primary: #ffb74d` داكن / `#9a6a1e` فاتح
- `--accent: #ff6f00` داكن / `#9a4f00` فاتح
- `--success: #66bb6a` داكن / `#2e7d32` فاتح
- `--danger: #ef5350` داكن / `#c62828` فاتح
- `--gold: #ffb300` داكن / `#8f6911` فاتح (نص ذهبي آمن على العاج)
- `--brand-gold: #d4af37` وسلّم `--gold-100…900` ثابت عبر الوضعين
- `--quantum-lime: #ffd54f` (قيمة tokens.css الفعلية؛ كان موثقًا سابقًا `#C8FF36` ولم يُستخدم أبدًا)
- الاشتقاقات التنفيذية (الأسطح الزجاجية، توهجات الهيرو، رموز المسابقة) في `apps/web/tokens.css`.

## Typography

- Readex Pro هو الخط الوحيد للعرض والنصوص.
- العناوين 700، النصوص 400، الأزرار والحالات 600–700.
- العنوان الرئيسي ضخم ومضغوط بصريًا، والنص التشغيلي أقصر وأكثر هدوءًا.
- الأرقام والرموز المهمة تستخدم `font-variant-numeric: tabular-nums`.

## Components

- الشعار علامة ذهبية مربعة بظل صلب، لا كرة توهج عامة.
- الزر الذهبي يملك ظلًا صلبًا قصيرًا؛ الزر السماوي يستخدم للحالة التقنية لا للمنافسة مع الدعوة الأساسية.
- اللوحات بحدود `rgba(255,255,255,.11)`، نصف قطر 16–24px، وظل عميق واحد.
- الجداول والنماذج تتبع Control Deck: صفوف واضحة، عناوين صغيرة، وحالات كبسولية لا تعتمد على اللون وحده.
- البطاقات تتغير في التركيب والحجم حسب المحتوى؛ لا شبكة من صناديق متطابقة بلا سبب.

## Motion

- ظهور الأقسام مرة واحدة عبر fade وslide قصير.
- hover يرفع العنصر 2px فقط ويغيّر الحد، من دون توهج دائم.
- النبض للحالة الحية، والدوران/الجسيمات للتتويج فقط.
- كل الحركة متوقفة عند `prefers-reduced-motion: reduce`.

## Responsive rules

- يبدأ التصميم من 320px، وتبقى أهداف اللمس 44px على الأقل.
- تتحول التقسيمات الثنائية إلى تدفق واحد، وتبقى الدعوة الأساسية قبل المعاينة على الجوال.
- الجداول الإدارية تستخدم تسميات صفية أو تمريرًا محسوبًا، ولا تُخفى المعلومات الحرجة.
- لا يوجد تمرير أفقي على مستوى الصفحة.

## Voice

مباشر، واثق، عربي من الأصل. الأزرار أفعال قصيرة مثل «أنشئ مسابقة» و«انضم برمز». لا تُضاف ادعاءات أو أرقام تجارية غير موثقة.

## Surface discipline

كل الصفحات تشترك في الخلفية والخط والشريط العلوي والأزرار والأسطح وحالات التركيز. صفحات التشغيل تقلل الزخرفة، بينما الرئيسية والبث والتتويج تحمل التعبير الكامل للهوية.

## Homepage — Ivory Crown

- الصفحة الرئيسية وحدها تنتقل إلى مشهد نهاري فاخر: عاج دافئ، حبر كحلي داكن، وذهب معتّق. أسطح اللعب الداخلية تبقى داكنة لتصل الهوية الجديدة بعالم المنصة الليلي.
- أول مشهد غير متماثل؛ اسم «تحدّي» كتلة سوداء كبيرة تقابلها علامة تاج هندسية داخل لوحة كحلية ذات حواف ذهبية.
- الزخرفة خطوط هندسية رفيعة ومحدودة، منفّذة بـCSS وSVG دون صور خلفية ثقيلة أو مؤثرات تمرير مكلفة.
- يعتمد النظام خط `Cairo` العربي الواضح، مع أوزان وتباعد مدروس يمنح اسم «أميرة» حضورًا أفخم من بقية النصوص دون تحميل خطوط إضافية.
- غرفة اللعب، الألعاب، الجولات، بنك الأسئلة، المزايا، ولوحة الشرف تبقى كاملة الوظائف وتُعرض في إيقاع لوحات متفاوت لا شبكة صناديق متطابقة.
- خاتمة الرئيسية تبدأ بإهداء مستقل إلى «أميرة»، ثم إسناد البناء والتصميم، ثم الحقوق والروابط القانونية، ولا تستخدم عبارة «صاحب الموقع».

export const categoryColors = {
  history: 'var(--gold)', science: 'var(--info)', sport: 'var(--success)',
  culture: 'var(--quiz-option-c)', technology: 'var(--primary)',
} as const;

export const iconSizes = { sm: 16, md: 20, lg: 24, display: 36 } as const;
